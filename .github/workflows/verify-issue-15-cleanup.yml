name: Verify issue 15 and cleanup branches
on:
  push:
    branches: [main]
    paths: [.github/workflows/verify-issue-15-cleanup.yml]
permissions:
  contents: write
jobs:
  verify-and-cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install --disable-pip-version-check pytest
      - run: python -m pytest tests/test_nand_collection_state.py -q
      - name: Delete merged work branches
        run: |
          for branch in agent/fix-nand-candidate-priority ops/finalize-issue-15; do git push origin --delete "$branch" || true; done
      - name: Remove one-time workflow
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git fetch origin main
          git checkout -B main origin/main
          git rm .github/workflows/verify-issue-15-cleanup.yml
          git commit -m 'chore: remove issue 15 verification workflow [skip ci]'
          git push origin HEAD:main
