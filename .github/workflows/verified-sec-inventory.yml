name: Verified SEC inventory extraction

on:
  workflow_dispatch:
  schedule:
    - cron: "41 */2 * * *"
  push:
    branches: [main]
    paths:
      - ".github/workflows/verified-sec-inventory.yml"
      - "scripts/extract_verified_sec_inventory.py"
      - "tests/test_verified_sec_inventory.py"
      - "data/earnings_ledger/events.ndjson"
  pull_request:
    paths:
      - ".github/workflows/verified-sec-inventory.yml"
      - "scripts/extract_verified_sec_inventory.py"
      - "tests/test_verified_sec_inventory.py"
      - "data/earnings_ledger/events.ndjson"

permissions:
  contents: write

concurrency:
  group: verified-sec-inventory-${{ github.ref }}
  cancel-in-progress: true

jobs:
  extract-verified-inventory:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install test dependency
        run: python -m pip install --disable-pip-version-check pytest

      - name: Run regression tests
        run: python -m pytest -q tests/test_verified_sec_inventory.py

      - name: Extract verified SEC XBRL inventory with retry
        env:
          SEC_USER_AGENT: ${{ vars.SEC_USER_AGENT }}
        shell: bash
        run: |
          set -u
          if [ -z "${SEC_USER_AGENT:-}" ]; then
            echo "Repository variable SEC_USER_AGENT is required."
            exit 1
          fi
          ok=0
          for attempt in 1 2 3; do
            echo "Company Facts inventory extraction attempt ${attempt}/3"
            if python scripts/extract_verified_sec_inventory.py; then
              ok=1
              break
            fi
            if [ "$attempt" -lt 3 ]; then
              sleep $((attempt * 30))
            fi
          done
          if [ "$ok" -ne 1 ]; then
            echo "verified SEC inventory extraction failed after 3 attempts"
            exit 1
          fi

      - name: Commit verified inventory artifact
        if: github.event_name != 'pull_request'
        shell: bash
        run: |
          if [ -z "$(git status --porcelain -- data/earnings_ledger/verified_inventory_latest.json)" ]; then
            echo "No verified inventory changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/earnings_ledger/verified_inventory_latest.json
          git commit -m "data: update verified SEC inventory"
          git push
